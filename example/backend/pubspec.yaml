name: backend
description: "The backend gRPC machine control example."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  unleashing_grpc:
    path: ../../
  dart_periphery: ^0.9.6
  event_bus: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
